[project]
name = "jev-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "dotenv>=0.9.9",
    "pydantic>=2.0.0",
    "typesafe-sdk>=0.7.0",
]
